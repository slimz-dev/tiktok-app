const images = {
	home: require('./home.svg').default,
	clearbutton: require('./clearbutton.svg').default,
	playbutton: require('./playbutton.svg').default,
	blacklogo: require('./blacklogo.svg').default,
};

export default images;
